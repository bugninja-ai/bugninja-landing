{
  "name": "bugninja-landing",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@bugninja/landing",
    "dev:landing": "npm run dev --workspace=@bugninja/landing",
    "build": "npm run build --workspace=@bugninja/landing",
    "build:landing": "npm run build --workspace=@bugninja/landing",
    "lint": "npm run lint --workspace=@bugninja/landing",
    "lint:landing": "npm run lint --workspace=@bugninja/landing",
    "type-check": "npm run type-check --workspace=@bugninja/landing",
    "type-check:landing": "npm run type-check --workspace=@bugninja/landing"
  },
  "dependencies": {
    "@alloc/quick-lru": "^5.2.0",
    "client-only": "^0.0.1",
    "react-markdown": "^10.1.0",
    "remark-gfm": "^4.0.1"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.16",
    "concurrently": "^8.2.2"
  }
}
